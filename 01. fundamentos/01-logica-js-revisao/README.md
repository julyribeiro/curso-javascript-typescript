# Fundamentos - Parte 1

Nesta etapa inicial, revisitei os conceitos básicos de como o JavaScript interpreta instruções e como organizamos a informação na memória.

## 🧠 Conceitos Estudados

### 1. Algoritmos e Pensamento Lógico
* **O que é um Algoritmo:** Entendi como uma sequência finita de passos para resolver problemas, podendo envolver tomadas de decisão e repetições.
* **Ciclo de Dados:** O fluxo fundamental de Entrada (Input) -> Processamento -> Saída (Output).
* **Representação:** A lógica pode ser expressa em linguagem natural, fluxogramas ou pseudo-códigos (Portugol) antes de virar código real.

### 2. Estrutura de Dados
* **Organização:** Como administrar os dados de forma eficiente através de listas, filas ou árvores.
* **Lógica de Fluxo:** * **FIFO:** Primeiro a entrar, primeiro a sair.
    * **LIFO:** Último a entrar, primeiro a sair.
* **Operações:** O uso de operadores aritméticos, relacionais e lógicos para manipular os dados armazenados.

### 3. Instruções e Estrutura de Código
* **Sentenças de Código:** A menor unidade de instrução (ex: `console.log`). Entendi que o ponto e vírgula é opcional em JS, mas ajuda na organização.
* **Blocos de Código:** Agrupamentos de sentenças delimitados por chaves `{}`. Essencial para isolar trechos de lógica e definir escopos.
* **Comentários:** Uso de `//` para linha única e `/* */` para múltiplas linhas, fundamental para documentar a intenção do código.

### 4. Manipulação de Dados
* **Variáveis (`let` vs `var`):** Entendi que o `let` é a forma moderna e segura de criar variáveis que permitem alteração de valor.
* **Constantes (`const`):** Espaços de memória para valores que não devem mudar (como o valor de PI).
* **Tipos de Dados:**
    * `Number`: Inteiros e decimais (ponto flutuante).
    * `String`: Textos com aspas simples ou duplas.
    * `Boolean`: Valores lógicos (`true` ou `false`).
* **Operador `typeof`:** Ferramenta para identificar o tipo de uma variável em tempo de execução.

---

### 🏆 Desafios Práticos
* **Cálculo de Área:** Aplicação de fórmulas matemáticas usando `const` e `let`.
* **Troca de Valores:** Exercício de lógica para inverter valores entre duas variáveis usando uma variável temporária ou desestruturação.

---

[Link para a pasta anterior](../../README.md)